tuple :元组（即常量数组）

tuple = ('a','b','c','d','e')
应用场景是用于一些不可变的量的时候，
方法很少：count() index
作用：有时候会有一些数据只有只读的作用（不可修改，没有其他的作用）
==========================元组和列表的相互转换==============
列表和元组的相互转化
>>> a = ('a', 'b', 'c', 'd', 'e')
>>> list(a)
['a', 'b', 'c', 'd', 'e']
 